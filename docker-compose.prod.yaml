version: "3.4"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    image: docker.prakashk.com/network/backend:master
    environment:
      - NODE_ENV=production

  frontend:
    build:
        context: ./frontend
        dockerfile: Dockerfile.prod
    image: docker.prakashk.com/network/frontend:master
    environment:
        - NODE_ENV=production

